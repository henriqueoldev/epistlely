---
const { pageId } = Astro.props;
---

<div class="bookPage" id={pageId}>
    <slot />
</div>

<style>
    .bookPage{
        max-width: 600px;
        text-align: left;
        line-height: 150%;
        padding: 1rem;
        background-color: var(--primary-color);
        border-radius: .5rem;
        display: none;
    }
</style>
